spring:
  application:
    name: ship-it
  security:
    user:
      name: ${ADMIN_USERNAME:admin}
      password: ${ADMIN_PASSWORD:admin}
      roles: ADMIN
app:
  sanity-checks-path: classpath:data/sanity-checks.json
